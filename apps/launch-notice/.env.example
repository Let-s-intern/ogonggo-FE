# 포켓베이스 주소. 로컬은 http://127.0.0.1:8090, 배포하면 그 주소.
POCKETBASE_URL=

# 슈퍼유저 계정. 신청 컬렉션의 규칙을 슈퍼유저 전용으로 잠가 두었기 때문에 필요하다 —
# 규칙을 열면 주소를 아는 누구나 남의 이름·이메일·연락처를 읽을 수 있다.
#
# 이 값들은 서버에서만 읽는다. NEXT_PUBLIC_ 을 붙이지 마라.
POCKETBASE_ADMIN_EMAIL=
POCKETBASE_ADMIN_PASSWORD=
