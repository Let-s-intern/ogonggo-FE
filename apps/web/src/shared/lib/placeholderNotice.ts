/**
 * 비활성 컨트롤에 다는 안내 한 줄. "왜 눌리지 않는가" 에 답한다.
 *
 * 마이페이지 화면 셋이 쓴다 — 지원·신청 내역의 하드코딩 두 탭(PRD 3 절), 작성한 모집글의
 * 작성·수정 버튼(PRD 4 절, 그 화면이 PRD 5 절이라 아직 없다), 개인 정보의 빠지는 칸
 * 일곱(PRD 6 절). 세 곳이 같은 말을 해야 해서 `shared` 에 둔다.
 *
 * 컨트롤을 감추지 않고 비활성으로 남기는 것이 PRD 의 결정이다. 자리가 통째로 비면 "이
 * 화면에는 그런 동작이 없다" 로 읽히는데, 사실은 준비 중이다.
 */
export const PLACEHOLDER_NOTICE = '준비 중인 기능이에요';
