# widgets

Compositions of entities and features into a reusable page section (a nav bar, a data table with
its filters). A widget may import from `features/`, `entities/` and `shared/`, never from `pages/`
or `app/`.
