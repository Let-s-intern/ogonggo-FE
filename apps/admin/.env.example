# 포켓베이스 주소. 출시알림 신청 목록을 여기서 읽는다.
# 비어 있으면 어드민 홈에 그 화면이 그려지지 않는다.
#
# 주소는 공개돼도 된다 — 컬렉션이 슈퍼유저 전용으로 잠겨 있어 화면에서 로그인해야 보인다.
#
# 런칭(2026-09-23) 뒤 apps/launch-notice 를 지울 때 이 변수도 같이 지운다.
VITE_POCKETBASE_URL=
